# Ohne TTY (z. B. Docker Compose `command: sh -c "pnpm install …"`) verlangt pnpm sonst eine Bestätigung
# zum Löschen von node_modules → Frontend-Container startet nicht (ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY).
confirm-modules-purge=false
